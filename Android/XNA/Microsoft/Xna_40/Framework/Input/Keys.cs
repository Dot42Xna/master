using System;
using Microsoft.SPOT;

namespace Microsoft.Xna.Framework.Input
{
    public enum Keys : byte
    {
        Space = (byte)' ',
        Left = 0x25,
        Up = 0x26,
        Right = 0x27,
        Down = 0x28,
        A = (byte)'A',
        S = (byte)'S',
        W = (byte)'W',
        D = (byte)'D',
    }
}
